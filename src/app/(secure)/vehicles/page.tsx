import * as React from 'react';
import VehiclesStrategyEntry from './VehiclesStrategyEntry';

export const metadata = {
  title: 'Vehículos | Mobix',
};

export default async function VehiclesPage() {
  return (
    <VehiclesStrategyEntry />
  );
}
