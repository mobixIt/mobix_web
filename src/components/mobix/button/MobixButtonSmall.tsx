'use client';

import { styled } from '@mui/material/styles';
import { MobixButton } from './MobixButton';

export const MobixButtonSmall = styled(MobixButton)(() => ({
  height: 40,
  borderRadius: 2,
}));
